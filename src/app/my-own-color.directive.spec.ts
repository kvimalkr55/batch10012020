import { MyOwnColorDirective } from './my-own-color.directive';

describe('MyOwnColorDirective', () => {
  it('should create an instance', () => {
    const directive = new MyOwnColorDirective();
    expect(directive).toBeTruthy();
  });
});
